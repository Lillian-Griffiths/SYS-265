[defaults]
inventory         = inventory.ini
host_key_checking = False       # Skips SSH host key prompts in the lab
timeout           = 30

[winrm]
transport         = basic       # Basic auth over HTTP for lab use
